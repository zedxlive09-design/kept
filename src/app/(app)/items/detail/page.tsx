/**
 * Kept — Item Detail (Phase 4)
 *
 * Reads ?id= via useSearchParams() — NOT a dynamic [id] segment.
 * See §3 for why: static export requires all routes known at build time.
 */
export default function ItemDetailPage() {
  return <p className="text-muted-foreground">Item Detail — Phase 4</p>;
}